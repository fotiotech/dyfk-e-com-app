{
  "name": "Dyfk",
  "version": "1.0.0",
  "homepage": "https://fotiotech.github.io/dyfk-e-com-app/",
  "scripts": {
    "start": "node api/app.js",
    "client": "cd client && yarn dev",
    "build": "cd client && yarn build",
    "deploy": "cd client && yarn build && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
